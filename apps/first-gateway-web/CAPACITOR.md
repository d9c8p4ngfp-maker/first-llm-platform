# Capacitor (Phase 5)

```bash
npm run build
npx cap init "First Gateway" com.first.gateway.console
npx cap add android
npx cap copy
npx cap open android
```

Frontend code requires no changes for Capacitor shell packaging.